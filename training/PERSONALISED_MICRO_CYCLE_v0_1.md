# Live Cash System — First Personalised Micro-Cycle v0.1

Status: `PRIOR-ONLY SHELL / T1 RESPONSE-ADAPTIVE`

## Honest boundary

The learner has declared high-value discomfort areas, but no strategic leak has yet been measured. Therefore this cycle fixes the measurement and branching order, not the final teaching order.

## Cycle length

Five short sessions, normally 15–25 minutes each.

## Session 0 — Cold baseline

- complete `LD-001` through `LD-010`;
- provide action/direction, one-sentence reason, confidence and rough time;
- receive no teaching until the tranche is complete.

Output:

- response classes;
- candidate posterior estimates;
- misconception evidence;
- confidence and latency flags;
- two highest-value repair families.

## Session 1 — Highest structural family

Generated from T1:

```text
one compact cue
→ one explanation
→ one positive example
→ one counterexample
→ two changed-node decisions
→ one delayed item scheduled
```

Maximum one new mechanism.

## Session 2 — Second structural family

Use the same structure. If T1 shows no clear second family, run mixed retrieval rather than inventing a weakness.

## Session 3 — MTT-to-cash transfer

Select one measured weak branch among:

- straddle/post-action geometry;
- BB/SB range source;
- OOP versus wide or value-heavy 3-bet;
- deep OOP protected call versus required raise;
- multiway sandwich/shared defence.

Use one 100–200bb node and one 200bb+ or straddled changed variant.

## Session 4 — Delayed retrieval and table compression

- cold retest from Session 1;
- cold retest from Session 2;
- one noisy mixed node;
- two table cues only;
- one Batumi observation mission;
- update the next cycle from evidence.

## Branch map

| T1 evidence | First repair |
|---|---|
| `MC-001` / `MC-002` | effective stack, straddle denominator and post-action SPR |
| `MC-005` / `MC-019` / `MC-020` | blind/range source and 3-bet ancestry |
| `MC-008` / `MC-012` / `MC-013` | deep OOP call/raise boundary |
| `MC-024` / `MC-027` | multiway action order and shared defence |
| `MC-015` / `MC-022` / `MC-028` | branch evidence and river ancestry |
| no repeated structural error | mixed speed/transfer test; do not teach foundations automatically |

## Load guard

- at most two repair families per cycle;
- at most one new mechanism per session;
- no exact chart memorisation;
- no promotion to `RETAINED` from this cycle alone;
- no global reset;
- no field exploit without opportunity-denominator evidence.

## Immediate next action

`RUN T1 / LD-001 … LD-010`

## Verdict

`FIRST_MICRO_CYCLE_SHELL_ACTIVE`

`PERSONALISED_BRANCH_PENDING_REAL_RESPONSES`
