# Live Cash System — Max-EV Candidate Priority Ranking v0.5

Status: `ACTIVE_DIAGNOSTIC_PRIOR / T1_PENDING / NO_FAKE_RERANK`

Supersedes `MAX_EV_CANDIDATE_PRIORITY_RANKING_v0_4.md`.

## Function

```text
spot frequency
× average error cost
× current learner-error probability
× transfer value
× learnability
× evidence confidence
```

The learner-error factor now has an executable measurement system but still has no responses.

## What changed

Created:

- 20-item original diagnostic battery;
- ten-item active T1;
- free-text action/reason/confidence/time capture;
- frozen misconception routing;
- shrinkage error-probability model;
- machine response schema and scorer;
- empty learner-state authority;
- response-adaptive five-session micro-cycle.

## Pre-response priority

Until T1 is scored, preserve the v0.4 neutral-prior deployment order. Do not convert user-reported discomfort into confirmed error probability.

The first diagnostic samples:

1. effective depth and straddle denominator;
2. blind source identity;
3. value-heavy and over-wide 3-bet branches;
4. deep OOP protected call versus protection raise;
5. small-wide versus large-polar response;
6. multiway sandwich/shared defence;
7. blocker ancestry and branch evidence.

## Rerank gate

Candidate reranking becomes permissible when:

- all ten T1 responses are evaluated;
- action and reasoning are separated;
- at least one changed-node pair is present;
- high-confidence structural errors are flagged;
- untested candidates remain `UNMEASURED`.

The first rerank remains `TENTATIVE`; stable estimates require delayed and variant evidence.

## Post-score order

```text
highest base-EV × posterior error estimate
→ prerequisite structural errors
→ high-confidence wrong models
→ changed-node transfer failures
→ slow but correct retrieval
→ low-confidence correct retrieval
```

At most two families enter the first repair cycle.

## Still unchanged

- 34 candidates;
- 29 `DRILL_READY`;
- 5 `VALIDATION_PENDING`;
- 34/34 direct drills;
- 0 admitted final rules.

## Next action

`RUN T1 / LD-001 THROUGH LD-010`

## Verdict

`DIAGNOSTIC_MEASUREMENT_READY`

`LEARNER_ERROR_PROBABILITY_NOT_YET_MEASURED`

`NO_FAKE_PERSONAL_RERANK`
