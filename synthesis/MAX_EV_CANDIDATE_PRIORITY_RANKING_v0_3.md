# Live Cash System - Max-EV Candidate Priority Ranking v0.3

Status: `ACTIVE_NEUTRAL_PRIOR / MULTIWAY_DIRECTIONAL_WAVE_COMPLETE / DIAGNOSTIC_UPDATE_REQUIRED`

Supersedes `MAX_EV_CANDIDATE_PRIORITY_RANKING_v0_2.md`.

## Max-EV function

```text
spot frequency
x average error cost
x current error probability
x transfer value
x learnability per unit time
x evidence confidence
```

Current learner-error probability remains incompletely measured.

## Completed closure lanes

### Lane 1 — preflop directional architecture

- five anchor cards;
- core/flex ranges;
- squeeze and polar-target direct drills;
- room/solver calibration still pending.

### Lane 2 — multiway directional architecture

- action-order roles;
- shared defence;
- OPAL ownership;
- player-behind gate;
- bluff-support tiers;
- field-clear transition;
- delayed-aggression direct answer key;
- field magnitude still pending.

## Deployment priority

### Deploy early

- effective depth and straddle denominator;
- preflop core/flex architecture;
- range source, filtering and ownership;
- multiway role and player-behind gate;
- bet shape and response geography;
- value-first aggression;
- 3-bet/4-bet ancestry;
- exact-branch exploit discipline.

### Deploy with context

- squeeze and polar target folds;
- multiway small-price audit;
- field-clear delayed aggression;
- fast-play/protect switch;
- river ancestry and blockers.

### Build/validate before broad use

- exact deep OOP protected-call boundary;
- target-room population magnitudes;
- exact solver edge frequencies;
- deep unequal-stack and side-pot multiway cases.

## New closure priority

### P0 — Deep OOP protected-call and exact depth/SPR boundary

Primary candidates:

- `H-W01-001`;
- `H-W01-006`;
- `H-R04-010`;
- `H-R05-002`.

Required outputs:

```text
robust versus frail hand map
-> static/dynamic board split
-> SPR and depth bands
-> call/raise/fold geometry
-> future-barrel source
-> direct H-W01-006 answer key
-> changed-node validation
```

This should close direct drill coverage to `34/34` if evidence supports a directional boundary.

### P1 — Learner diagnostics

Measure:

- preflop anchor errors;
- heads-up transfer into multiway;
- initiative entitlement;
- sandwich/closing-role confusion;
- blocker and random-air misuse;
- OOP discomfort raises;
- action/reason latency and confidence.

Update priority only for affected candidates.

### P2 — Target-live field calibration

Use opportunity denominators for:

- rake/open size;
- squeeze and 3-bet response;
- multiway tiny-bet elasticity;
- closing-player raises;
- field-clear turn leads;
- passive-pool slow-play costs;
- river underbluff branches.

### P3 — Targeted solver/visual calibration

Only review exact cells or run independent solves when they can change:

- a frequent table action;
- a direct answer key;
- a depth/context boundary;
- final compression/admission.

Do not build exhaustive chart libraries.

### P4 — Final compression and admission

Use:

`MINIMUM COMPLEXITY SUBJECT TO NO MATERIAL EV LOSS`

## Multiway priority effect

`H-W03-007`, `H-W03-008`, `H-W03-010` and `H-R04-007` move from build-first to deploy-with-context. `H-W03-009` remains field-sensitive because the direction is stable but the fast-play magnitude is not.

No candidate status or final admission changes solely because the architecture is now available.

## Verdict

`MAX_EV_NEUTRAL_PRIOR_V0_3_ACTIVE`

`PREFLOP_AND_MULTIWAY_DIRECTIONAL_LANES_COMPLETE`

`DEEP_OOP_IS_NEXT_HIGHEST_VALUE_CLOSURE_LANE`

`DIAGNOSTICS_MUST_UPDATE_FINAL_SEQUENCE`
